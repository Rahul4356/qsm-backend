name: Deploy to Azure

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y cmake gcc g++ libssl-dev ninja-build
    
    - name: Build and install liboqs
      run: |
        git clone --depth 1 https://github.com/open-quantum-safe/liboqs.git
        cd liboqs
        mkdir build && cd build
        cmake -GNinja -DBUILD_SHARED_LIBS=ON ..
        ninja
        sudo ninja install
        sudo ldconfig
    
    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install cython numpy
        git clone --depth 1 https://github.com/open-quantum-safe/liboqs-python.git
        cd liboqs-python
        python setup.py install
        cd ..
        pip install -r requirements.txt
    
    - name: Test OQS installation
      run: |
        python -c "import oqs; print('OQS Version:', oqs.oqs_version())"
    
    - name: Run tests
      run: |
        python -c "from service_quantum import test_quantum_crypto; test_quantum_crypto()"
    
    # Docker build (optional - since Azure Web App can run Python directly)
    - name: Build Docker image
      run: |
        docker build -t qms-quantum:latest .
    
    # Deploy to Azure Web App (if you have it configured)
    # - name: Deploy to Azure Web App
    #   uses: azure/webapps-deploy@v2
    #   with:
    #     app-name: your-app-name
    #     publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}